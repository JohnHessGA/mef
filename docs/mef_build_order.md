# MEF Build Order

Version: 2026-04-21

Living checklist of MEF build milestones. Mirrors `docs/README_mef.md`
§"Build Order" and `docs/mef_design_spec.md` §20; updated as milestones
land so any future reader can see where we are without grepping commits.

| Status  | # | Milestone                         | Key deliverables                                                                                                                                                         |
|---------|--:|-----------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| ✅ done | 1 | Repo + database setup             | `~/repos/mef/` skeleton, MEFDB + `mef_user`, `sql/mefdb/001_schema_and_core_tables.sql` (12 tables), `mef status`, `mef init-db`                                         |
| ✅ done | 2 | Universe load                     | `mef.universe_stock` (305) and `mef.universe_etf` (15); idempotent loader from the `notes/` markdown files; `mef universe show` + `mef universe load`                    |
| ✅ done | 3 | Skeleton daily run                | `mef run --when {premarket\|postmarket}`, `daily_run` row, rendered email body, UID generator, per-run email layout                                                      |
| ✅ done | 4 | Evidence pull + ranker v0         | `mef.evidence` reads `mart.stock_equity_daily` + `mart.stock_etf_daily`; `mef.ranker` writes `candidate` rows and promotes survivors to `recommendation` state `proposed` |
| ✅ done | 5 | LLM gate                          | Claude-CLI integration (`mef.llm_trace`), approve/reject prompt, `mef.candidate.llm_gate_decision` + `llm_gate_reason` audit trail, fallback on LLM failure               |
| ✅ done | 6 | Position tracking                 | `mef import-positions <csv>`, sha256-deduped `mef.import_batch`, per-position `position_snapshot`, auto-activation of proposed recs when a matching holding appears       |
| ✅ done | 7 | Recommendation lifecycle commands | `mef dismiss`, `mef recommendations`, `mef show`, auto-expiration at entry window end, auto-close-on-disappearance with win/loss/timeout classification from last known price |
| ✅ done | 8 | Scoring                           | `mef score`, `mef.score` rows, estimated 100-share P&L, SPY + sector-ETF benchmark comparisons, refined outcome classification, plus `mef rejections` audit command       |
| ✅ done | 9 | Email delivery                    | Direct SMTP via `smtplib` (uses MDC's `notifications.yaml` for credentials, MEF's own recipients), `--dry-run` flag for preview, two-entry cron template at `cron/mef.cron`, install docs at `docs/mef_cron.md`. **Not** via notify.py — that script forces its own subject/body wrapper which would mangle MEF's two-section daily report. |
| ✅ done | 10 | Overwatch telemetry              | `ow.mef_run` (one row per run with full counts + duration + email status) and `ow.mef_event` (info/warning/error events) live; writes are fail-silent so an overwatch outage never breaks a run. Grafana dashboards still TBD (milestone 11). |
| ✅ done | 11 | Validation infrastructure        | **Data-freshness gate** (`evidence.check_freshness`, banner-or-abort based on age of latest mart bar). **Shadow scoring** (`mef.shadow_score` forward-walks every LLM-rejected candidate). **Paper trading** (`mef.paper_score` does the same for every emitted rec; reuses `classify_walk()` so methodologies match). **`mef gate-audit` command** (4-column comparison: Approved / Review / Rejected / Unavailable; honest sample-size discipline). Collapses validation horizon from "wait for John to buy something" (months) to "wait for time_exit to elapse" (weeks). |
| ✅ done | 12 | 3-way LLM gate + new prompt      | Disposition expanded approve/reject → **approve / review / reject**, with server-validated `issue_type` enum (mechanical / risk_shape / volatility_mismatch / posture_mismatch / asset_structure / options_structure / missing_context / none). New conservative prompt with explicit review order. Email shows only approved + unavailable; review-tagged recs are saved and auto-activate but stay out of the email. New `llm_gate_issue_type` column on `mef.candidate`. Live verified: today's 0/3/2 disposition shows the LLM correctly using `review` for "extended momentum, no pullback" setups. |
| ✅ done | 13 | Activation provenance            | `mef.recommendation.provenance` enum (`mef_attributed` / `pre_existing` / `independent`), inferred at activation time from the symbol's earliest position-snapshot date relative to the rec's creation date and entry window. `mef tag <rec-uid> --provenance ...` user override. Lets future audits separate MEF-attributed wins from ambient ones the user would have made anyway. |
| ✅ done | 14 | Daily P&L tracking + link-trade  | `mef.recommendation_pnl_daily` time-series table — one row per (rec_uid, as_of_date) for every active rec, plus a close-day row for newly closed (so each series has a clean endpoint). Idempotent via ON CONFLICT. New nullable columns on `mef.score` for actual realized P&L (qty, buy/sell prices+dates, realized_pnl_usd, **realized_pnl_per_day** — the headline metric for "max profit in shortest time"). `mef link-trade` CLI bridges manual trade data until PHDB transaction history exists. |
| ✅ done | 15 | Report + show polish             | `mef report --when {pre,post}market [--run UID]` rebuilds the email body from existing DB state for any historical run (no LLM call, no SMTP). `mef show` now surfaces provenance, issue_type, paper-score outcome, realized scoring (with real-vs-estimated P&L), and the full daily P&L curve.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ✅ done | 16 | Layered gating refactor           | Split monolithic `_score_*` into Layer A / B / C. New `mef.eligibility` (per-engine earnings blackout) + `mef.hazard_overlay` (macro + earnings-proximity). FCF hard veto moved out of trend/mean_rev and retained in value-engine Layer C only. `raw_conviction` + `conviction_score` (final) split; hazard-suppressed candidates persisted as first-class records and shadow-scored. Migration `sql/mefdb/011_layered_gating.sql`. Canonical doc: `docs/mef_layered_gating.md`. |
| ⏳ next | 17 | Iteration                         | Continued LLM-prompt refinement based on real audit data, evidence-weight tuning once scoring history accumulates (~30+ closes), Grafana MEF dashboards over `ow.mef_*` tables, optional `mef.benchmark_snapshot` cache if SHDB joins prove slow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 🅿 parked | 18 | Universe management              | Per-symbol audit + suggested adds/removes after ~3 months of scoring history. Quarterly cadence. Explicitly waiting for data — premature without it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

Items past #16 that remain out of scope for v1 (web UI, DAS integration, RSE integration, long-option scoring, broader evidence families) — see `docs/README_mef.md` §"Hard Boundaries".

## Status notes

- **Threshold + cap** currently default to `conviction_threshold=0.5`, `max_new_ideas_per_run=5` in `config/mef.yaml` (gitignored). Threshold lowered 0.6→0.5 once on 2026-04-19 to widen the candidate pool that competes for the top-5 cap. **From here it goes UP** as scoring evidence accumulates — see `CLAUDE.md` core principle #7.
- **Coverage** at last verification: 305/305 stocks and 15/15 ETFs present in `mart.stock_equity_daily` / `mart.stock_etf_daily` as of 2026-04-17.
- **First real run** (DR-000003): 320 symbols evaluated, 158 non-no_edge candidates, 5 emitted (STX, GEV, GLW, KLAC, LITE).
- **First 3-way-gate run** (DR-000013, 2026-04-20): 0 approve / 3 review / 2 reject. LLM correctly used `review` with `issue_type=risk_shape` for "extended momentum, no pullback" setups (LITE +27%, STX +25% in 20d). Demonstrates the discriminating value of the new disposition vs the prior binary gate, which would have rejected outright.
- **Audit corpus** as of 2026-04-20: 14 deferred shadow-scores (rejected candidates), 26 deferred paper-scores (emitted recs); none yet settled. Full picture comes online once `time_exit` dates start passing in early-to-mid May 2026.
- **2026-04-21 ranker iteration** (milestone #16 work). Two-week block of ranker tuning driven by user review of live top-5 output. Shipped in this order:
  - `c1834e1` bias away from "already ran up" names — tiered `return_20d` reward, extension-from-SMA50 penalty
  - `3439d40` fix evidence pulling 10-day-old bars against large universe arrays (TimescaleDB planner quirk with `DISTINCT ON + ANY()`; rewritten as CTE-with-MAX)
  - `845610e` pullback-aware entry zones — `needs_pullback` flag at `drawdown > -0.03` anchors entry below close on at-peak picks; ⏳ email annotation
  - `8f9d1b8` LLM gate learns about `pullback_setup=true` so it stops flagging intentional below-current entries as `risk_shape`
  - `088f32d` email renders LLM-review-tagged ideas in their own section with the LLM's reason visible
  - `2f0c700` SMA-slope chop detection (|slopes| flat → range_bound; rising SMA20 bonus / rolling-over penalty)
  - `368d302` multi-timeframe consensus with V-recovery-aware thresholds — counts "strong disagreements," not "perfect alignment," so normal recovery-window negativity doesn't penalize good stocks; 5d tactical brake for falling-knife cases
  - `501607d` volatility contraction (`rv20d/rv63d < 0.80` → coiled) — positive signal for "ready to run," not just "already ran"
  - `efab466` relative strength beyond SPY — `rs_vs_spy_63d`, `rs_vs_qqq_63d`, and sector-relative 63d via `SECTOR_TO_ETF`
  - `b705290` fundamental sanity — `free_cash_flow < 0` is a hard veto; `pe_trailing > 60` and low `earnings_yield` are soft penalties (stocks only; ETFs fall through)
  - `8d09661` LLM prompt candidate block expanded (ret5d/63d/252d, sma20_slope, rv20/rv63, rs_vs_spy_63d, rs_vs_qqq_63d) so the LLM sees the same signals the ranker weighs
  Preview against 2026-04-17 evidence: JCI climbed from conv 0.80 → 0.97 (all signals aligned); TSLA dropped from top to #10 (252d damage + multi-timeframe); the extended names that drove the original complaint (STX, LITE, CSCO, CSX) left the top 5 entirely.
- **Known ranker blind spots for next iteration pass** — "falling knife" still only caught via the 5d brake, not sma_20 direction in the `needs_pullback` path; range-bound-above-SMAs catches most but not all chop (BMY still surfaced on 2026-04-21 preview — slope threshold may need tightening once data supports a pick).
- **2026-04-21 three-engine ensemble** (milestone #16 work, user-requested after the event-date ship). The single trend-follower is now one of three engines. Same deterministic-first / LLM-as-gate / "no new trades today always valid" discipline, but the ensemble surfaces setups the trend-follower structurally could not.
  - `960e116` scaffolding: `mef.candidate.engine` column + `mef.recommendation.source_engines` array (migration 009); engine registry pattern; `RankedCandidate.engine` field; backward-compatible `rank()` defaulting to all engines.
  - `4c622d7` mean-reversion engine: oversold bounce setups (RSI < 40, 5-15% below SMA50, bouncing not falling). Falling-knife veto at `return_5d < -2%` or drawdown < -30%.
  - `f9f022d` value engine: cheap + durable. PE 5-25, positive FCF, modest-positive 252d trend. Equities-only. Wider stop (10%), longer time_exit (60d).
  - `6cda2b1` multi-engine LLM prompt: candidate block shows per-engine scores; new SPECIAL RULE section explains that engine agreement is signal; output schema adds a `synthesis` array (LLM's ordered top-picks across engines). `_parse_gate_response` returns (reviews, synthesis). Migration 010 widens posture CHECK.
  - `c7491bd` email rendering: per-engine `[engine: X]` / `[engines: X+Y]` lineage badges on each item; new "Engine views" section at the bottom with each engine's top-3; actionable picks reordered by the LLM's synthesis.
  - (next commit) telemetry: `mef.shadow_score` / `mef.paper_score` INSERT statements include the `engine` column so audit queries can ask "which engine's conviction predicts wins best?"
  - Live dry-run verification (DR-000023/024/025): three engines produce entirely disjoint top-3s against 2026-04-17 evidence — trend (JCI/TJX/ACGL), mean-reversion (PSX/SYY/TMUS), value (TGT/MRK/PFE). LLM synthesis reorders the actionable section; per-engine audit infrastructure in place.

- **2026-04-21 event-date awareness** (MEF commit `e429505` + UDC commit `d2eb207`). Shipped after the ranker-tuning block above. Adds:
  - Earnings proximity gate: graduated veto/penalty/flag based on `next_earnings_date` from `shdb.earnings_calendar_upcoming` (99.3% universe coverage, FMP source). Hard veto at ≤5d general / ≤10d for pullback setups. -0.15 penalty at 6-10d. -0.03 flag at 11-21d. Email annotates each idea with `📅 earnings in Nd` when ≤21 days.
  - Macro-calendar overlay: -0.05 dampener when a US High-impact event lands today or tomorrow. Sources `shdb.economic_calendar` (UDC commit widened it to ingest forward dates; prior state was past-only). Email header banner renders upcoming events in a 3-day horizon.
  - Live verification against 2026-04-17 evidence: SLB (earnings in 7d) dropped out of top-25 via -0.15 penalty; BMY/ACGL/ALL/JCI flagged with caution for earnings 11-19d out; Housing-Starts-today triggered the macro dampener for all emittable candidates.
  - **First full pipeline run with F+G active (DR-000020, dry-run):** 3 approve / 1 review / 1 reject. The LLM gate explicitly cited the new `days_to_earnings` signal in two decisions — approved TGT with *"no earnings within window"* and rejected ACGL with *"pending earnings"* (11 days out) alongside the vol_z concern. TJX review cited *"flat relative strength"* (also a new signal). Confirms the LLM is actively using the expanded candidate block, not ignoring it.
  - Also fixed: `fix: Decimal features broke candidate JSON serialization` (commit `66e83fe`) — new mart columns (PE, FCF, realized_vol_63d, rs_vs_*) return as psycopg2 Decimals; `_json_safe` only handled date-like objects. Fix added a Decimal→float branch. Silent prior to the first live run because tests use plain floats.
  - Event-date families deliberately **not** added (ex-dividends, FOMC as dedicated signal, news-volume overlays, options expiration, post-earnings drift) — see `mef_out_of_scope.md` for the rationale of each.
- **2026-04-21 email UX polish** (milestone #17 work, post-layered-gating).
  - `7a5b0d5` surface `Rec ID: R-0000xx` on every idea — the closing `mef show <rec-id>` hint now actionable.
  - `1ee02b6` post-emission price-freshness check (`mef.price_check`). Fetches a live Yahoo quote on the ~5–10 emitted ideas and renders a "Price check:" line when the live price has moved ≥1% from the SHDB close used at scoring time. Warn tier (≥3%) carries "entry zone may need refresh." Never changes conviction or plan. Live catch on first run: MRK came back at $112.48 vs scored close $119.07 (−5.5%) — email now warns the user the zone is stale.
  - `17191a6` LLM timeout retry + reasoned banner. Retries once (60s pause, 180s timeout) when first attempt times out; falls through with a classified `unavailable_kind` (`timeout` / `parse` / `error`) that drives the email banner's "due to …" suffix.
  - `2f76fe0` idea header gains live price + `:etf` tag: `JCI ($139.79) · high — bullish — buy_shares`. ETFs carry the tag so they're spotted at a glance.
  - `2483752` conviction tier tag on every header (`high` ≥ 0.70, else `medium`); top-of-email Summary block with final-list tier split, cross- vs single-engine confirmations, held-for-review count; action-word rename on detail lines (`Buy near` / `Sell below` / `Sell above` / `Suggested hold: through YYYY-MM-DD`) with aligned 15-col labels.

Update this table whenever a milestone flips status.
